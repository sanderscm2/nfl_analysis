# Mermaid Test

If you see a flowchart below, Mermaid is working:

```mermaid
graph LR
    A[Start] --> B[Middle]
    B --> C[End]
    style A fill:#0066CC,color:#fff
```

If you just see code blocks, you need to install a Mermaid extension in VS Code.
